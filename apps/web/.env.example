# Jennifer City web — public env (safe to commit as example)
# Copy to apps/web/.env.local for local overrides.

# Public API origin for Phaser bridges (no trailing slash)
NEXT_PUBLIC_JENNIFER_API_URL=http://127.0.0.1:3001
