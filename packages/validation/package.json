{
  "name": "@jennifer/validation",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime Validation Engine – validation pipeline, confidence scoring, reality verification",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json --watch",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test $(find dist -type f -name '*.test.js')",
    "governance-validation": "pnpm run test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@jennifer/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "@typescript/native": "npm:typescript@^7.0.2"
  }
}
